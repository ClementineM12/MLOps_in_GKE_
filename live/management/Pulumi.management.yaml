config:
  project: gcp-management
  clusterName: management-cluster
  nodePool:
    machineType: e2-medium
    diskSizeGb: 100
    diskType: pd-standard
    minNodeCount: 1
    maxNodeCount: 3
    preemptible: false